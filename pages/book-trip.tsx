import { NextPage } from 'next';

const BookTrip: NextPage = () => {
    return (
        <div>
            <h1>Забронировать поездку</h1>
            {/* Список доступных поездок */}
        </div>
    );
};

export default BookTrip;
